(defmacro lambda (args body)
  (let ((head (car args))))
    (let ((tail (cdr args)))
      (let ((tailfun (cons (cons 'fun tail) body)))
         (if (nil? tail);(nil? head)
          (cons (cons 'fun head) body);(cons 'fun tailfun)
          (cons (cons 'fun tailfun) body
           ; (cons 'fun tailfun)  
          )
        )
      )
    )
)

; en regardant la définition de multi-macro
(defmacro lambda (args body)
  (let ((head (car args))))
    (let ((tail (cdr args)))
      (let ((tailfun (cons (cons 'fun tail) body)))
        (if (nil? head)
          (cons 'fun tailfun)
          (cons (cons 'fun head)
            (cons 'fun tailfun)  
          )
        )
      )
    )
)